import mode
import medienfinder
import range
import mean

l= input('enter with a comma and space between')

print("mean: "+str(mean.mean(l))+" medien: "+str(medienfinder.medein(l))+" mode: "+str(mode.mode(l)))

